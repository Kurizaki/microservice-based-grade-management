{
  // Custom application-level settings
  "AppSettings": {
    // Used as the secret key for creating and validating JWT tokens.
    // Keep this value secure; never commit real secrets to public repositories.
    "Token": "sadfghjkhgfdsewrtasdfgdsafgdasdfgshddsafghjdsafghjfdsafghjkfdsafghjk"
  },

  // Database connection strings
  "ConnectionStrings": {
    // Points to a local SQLite database file named AUTHDatabase.sqlite
    "DefaultConnection": "Data Source=AUTHDatabase.sqlite"
  },

  // Logging configuration
  "Logging": {
    "LogLevel": {
      // "Default" applies to all loggers that do not have a more specific category.
      "Default": "Information",
      // "Microsoft.AspNetCore" applies specifically to ASP.NET Core logs.
      // "Warning" hides verbose or informational logs from the ASP.NET Core framework.
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Allowed hosts for the application
  // The asterisk "*" means any host can access this application.
  "AllowedHosts": "*"
}
